language: python
services:
  - docker
before_install:
  - sudo apt-get update
  - pip install pycodestyle
  - pip install pytest-cov
  - pip install python-coveralls
install:
  - ls -d -1 "$PWD/"*.*
  - mkdir -p /home/travis/mycroft/skills/homeassistant.mycroftai
  - mkdir -p /home/travis/mycroft/config
  - cp -R /home/travis/build/Tony763/mycroft-homeassistant/* /home/travis/mycroft/skills/homeassistant.mycroftai/
  - cd /home/travis/mycroft/skills/homeassistant.mycroftai/
  - ls -d
  - docker pull homeassistant/home-assistant:0.114.3
  - docker run -d -p 127.0.0.1:8123:8123 --name="home-assistant" -v /PATH_TO_YOUR_CONFIG:/home/travis/mycroft/skills/homeassistant.mycroftai/test -v /etc/localtime:/etc/localtime:ro homeassistant/home-assistant:0.114.3
  - docker pull mycroftai/docker-mycroft
  - docker run -d --device /dev/snd -e PULSE_SERVER=unix:${XDG_RUNTIME_DIR}/pulse/native -v ${XDG_RUNTIME_DIR}/pulse/native:${XDG_RUNTIME_DIR}/pulse/native -v ~/.config/pulse/cookie:/root/.config/pulse/cookie -p 8181:8181 --name mycroft mycroftai/docker-mycroft
  - docker exec mycroft /bin/bash -c "mkdir /opt/mycroft/skills/homeassistant.mycroftai"
  - docker cp /home/travis/mycroft/skills/homeassistant.mycroftai mycroft:/opt/mycroft/skills/
  - echo "Wait for start of HA and Mycroft in background"
python:
  - "3.6"
script:
  - echo "Coding style check"
  - pycodestyle --max-line-length=100 __init__.py
  - pycodestyle --max-line-length=100 ha_client.py
  - echo "VKtest"
  - docker exec mycroft /bin/bash -c "cd /opt/mycroft/skills; ls -a;ls -d; cd /opt/mycroft/; source /opt/mycroft/.venv/bin/activate; /opt/mycroft/bin/mycroft-skill-testrunner vktest clear --allow-root; /opt/mycroft/bin/mycroft-skill-testrunner vktest -t homeassistant.mycroftai --allow-root; sleep 2m;"
after_success:
  - codecov
